{% extends 'app/layout.html.twig' %}

{% block content %}
    <div class="content">
        <div class="container">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb bg-light border">
                    <li class="breadcrumb-item"><a href="{{ path('app') }}">Accueil</a></li>
                    <li class="breadcrumb-item active" area-current="page">Plan du site</li>
                </ol>
            </nav>

            <div class="row">
                <div class="col">
                    <div class="card">
                        <div class="card-body">
                            <div class="mb-5">
                                 <h4 class="text-center text-muted mb-3">
                                     {{ 'à'|upper }} propos de EPE France
                                 </h4>
                                 <ul class="list-unstyled d-flex flex-row align-items-center justify-content-center">
                                     <li class="p-3"><a href="{{ path('app_about') }}">A propos</a></li>
                                     <li class="p-3"><a href="{{ path('app_contact') }}">Contactez nous</a></li>
                                     <li class="p-3"><a href="{{ path('app_cgu') }}">Conditions générales d'utilisation</a></li>
                                     <li class="p-3"><a href="{{ path('app_rgpd') }}">RGPD</a></li>
                                     <li class="p-3"><a href="{{ path('app_legal') }}">Mentions légales</a></li>
                                 </ul>
                            </div>
                            <div class="mt-3">
                                <h4 class="text-center text-muted mb-3">
                                    Liste des élus de France
                                </h4>
                                <ul class="list-unstyled d-flex flex-row flex-wrap align-items-center justify-content-center">
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-maires'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="9">
                                            <button class="btn btn-link text-primary">
                                                Maires
                                            </button>
                                        </form>
                                    </li>
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-conseillers-municipaux'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="1">
                                            <button class="btn btn-link text-primary">
                                                Conseillers municipaux
                                            </button>
                                        </form>
                                    </li>
                                    </li>
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-conseillers-communautaires'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="2">
                                            <button class="btn btn-link text-primary">
                                                Conseillers cantonaux
                                            </button>
                                        </form>
                                    </li>
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-conseillers-departementaux'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="3">
                                            <button class="btn btn-link text-primary">
                                                Conseillers départementaux
                                            </button>
                                        </form>
                                    </li>
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-presidents-des-departements'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="10">
                                            <button class="btn btn-link text-primary">
                                                Présidents de département
                                            </button>
                                        </form>
                                    </li>
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-conseillers-regionaux'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="4">
                                            <button class="btn btn-link text-primary">
                                                Conseillers régionaux
                                            </button>
                                        </form>
                                    </li>
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-presidents-des-regions'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="11">
                                            <button class="btn btn-link text-primary">
                                                Présidents de région
                                            </button>
                                        </form>
                                    </li>
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-conseillers-corse'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="5">
                                            <button class="btn btn-link text-primary">
                                                Conseillers corse
                                            </button>
                                        </form>
                                    </li>
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-depute'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="8">
                                            <button class="btn btn-link text-primary">
                                                Députés
                                            </button>
                                        </form>
                                    </li>
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-deputes-europeens'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="6">
                                            <button class="btn btn-link text-primary">
                                               Députés européens
                                            </button>
                                        </form>
                                    </li>
                                    <li>
                                        <form action="{{ path('elected_members_list', {categoryName: 'liste-des-senateurs'}) }}" method="post">
                                            <input type="hidden" name="categoryId" value="7">
                                            <button class="btn btn-link text-primary">
                                               Sénateurs
                                            </button>
                                        </form>
                                    </li>
                                </ul>
                            </div>

                             
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

